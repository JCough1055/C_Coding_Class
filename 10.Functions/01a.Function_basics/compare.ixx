module;

export module compare;

namespace it_1 {
    export int maximum(int a,int b){

        if(a>b)
            return a;
            else
            return b;
    }
}
namespace it_2{

   export int maximum(int a, int b);
    
}